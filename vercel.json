{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "bun run code:build",
    "devCommand": "bun run dev",
    "installCommand": "bun install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}