{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "bun code:build",
    "installCommand": "bun install",
    "outputDirectory": "out"
}