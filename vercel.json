{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "bun install && bun code:build",
    "installCommand": "bun install",
    "outputDirectory": "out"
}